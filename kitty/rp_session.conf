os_window_state maximized

# Workspace 1: Development
new_tab DEV
layout grid
# Terminal 1: VS Code + Jump
launch zsh -ic "dcup && dcinw"
# Terminal 2: Neovim
launch zsh -ic "dcinw nvim"
# Terminal 3: Codex
launch zsh -ic "dcinw codex"
# Terminal 4: Claude
launch zsh -ic "dcinw claude"

# Workspace 2: Runtime
new_tab RUNTIME
layout grid
launch zsh -ic "dcinw"
launch zsh -ic "dcinw"
launch zsh -ic "dcinw"
launch zsh -ic "dcinw"
